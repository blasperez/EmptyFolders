# proyectoMJ

Static GitHub Pages build for the Movil Jaguar USA website.

## Notes

- `index.html` is the English version.
- `es.html` is the Spanish version.
- The quote form is visual/demo-only on GitHub Pages.
- Live email sending requires the backend version in `server.js`.

## GitHub Pages

Publish from the `main` branch and the repository root.
